<tr>
  <td><%= tool_supplier.name %></td>
  <td>
    <%= link_to t('edit'), edit_tool_supplier_path(tool_supplier),
      class: 'btn btn-small btn-primary' if can? :update, ToolSupplier %>
    <%= link_to t('destroy'), tool_supplier_path(tool_supplier),
      method: 'delete', confirm: t('destroy_confirm'),
      class: 'btn btn-small btn-danger' if can? :destroy, ToolSupplier %>
  </td>
</tr>
