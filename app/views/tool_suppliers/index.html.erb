<div class="span9 offset2">
  <%= render 'tool_types/nav' %>

  <%= link_to t('new'), new_tool_supplier_path, class: 'btn btn-success pull-right' if can? :create, ToolSupplier %>
  <h3><%= ToolSupplier.model_name.human %></h3>

  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
        <th><%= ToolSupplier.human_attribute_name('name') %></th>
        <th><%= t('actions') %></th>
      </tr>
    </thead>
    <tbody>
      <%= render @tool_suppliers %>
    </tbody>
  </table>
</div>
