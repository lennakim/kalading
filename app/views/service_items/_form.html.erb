<%= simple_form_for @service_item, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :price %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                service_items_path, :class => 'btn' %>
  </div>
<% end %>
