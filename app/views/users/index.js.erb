$("#order_engineer_id").html("<%= escape_javascript(options_for_select(@users.map {|u| [u.name_and_order_num, u.id]})) %>")

$("#complaint_complained_id").html("<%= escape_javascript(options_for_select(@users.map {|u| [u.initial_and_name, u.id]})) %>")

$("#complaint_handler_id").html("<%= escape_javascript(options_for_select(@handler.map {|u| [u.initial_and_name, u.id]})) %>")

