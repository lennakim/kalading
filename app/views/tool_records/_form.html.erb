<%= simple_form_for(@tool_record) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :engineer_name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
