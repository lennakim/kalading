$("#district-select").html("<%= escape_javascript( options_for_select(@city.districts.asc(:name).map {|p| [p.name, p.id]})) %>").val("<%= @city.districts.asc(:name).first.id %>");
$('#order_address').val($("option:selected", $('#order_city_id')).text() + $("option:selected", $("#district-select")).text());